--- /Users/yogapermana/Desktop/vite-vue-template/src/components/ui/input/Input.vue	2026-06-09 13:24:25
+++ /Users/yogapermana/Desktop/versace/src/components/ui/input/Input.vue	2026-07-05 13:59:18
@@ -1,7 +1,7 @@
 <script setup lang="ts">
-  import type { HTMLAttributes, InputHTMLAttributes } from 'vue'
+  import type { HTMLAttributes } from 'vue'
   import { useVModel } from '@vueuse/core'
-  import { inject, computed } from 'vue'
+  import { inject } from 'vue'
   import { cn } from '@/utils/cn'
 
   const props = defineProps<{
@@ -11,7 +11,6 @@
     unstyled?: boolean
     readonly?: boolean
     disabled?: boolean
-    type?: InputHTMLAttributes['type']
   }>()
 
   const emits = defineEmits<{
@@ -23,16 +22,14 @@
     defaultValue: props.defaultValue,
   })
 
+  // Check if inside InputGroup by looking for parent data-slot
   const isInsideGroup = inject('input-group', false)
-
-  const isNumberInput = computed(() => props.type === 'number')
 </script>
 
 <template>
   <input
     v-model="modelValue"
     data-slot="input"
-    :type="props.type"
     :readonly="readonly"
     :disabled="disabled"
     :tabindex="readonly ? -1 : undefined"
@@ -42,8 +39,6 @@
         'w-full min-w-0 bg-transparent text-sm leading-5 text-neutral-900 outline-none',
         'placeholder:text-neutral-400',
         'disabled:cursor-not-allowed disabled:text-neutral-400',
-        // Hide spinner for number inputs
-        isNumberInput && 'no-spinner',
         // Standalone styles (not inside group)
         !unstyled &&
           !isInsideGroup && [
