--- /Users/yogapermana/Desktop/vite-vue-template/src/components/shared/search-input/SearchInput.vue	2026-07-07 22:11:04
+++ /Users/yogapermana/Desktop/versace/src/components/shared/search-input/SearchInput.vue	2026-07-05 13:59:18
@@ -12,8 +12,6 @@
     disabled?: boolean
     variant?: 'default' | 'glass'
     clearable?: boolean
-    /** Height preset: 'md' (h-11, default) or 'sm' (h-10 for toolbars) */
-    size?: 'sm' | 'md'
   }
 
   const props = withDefaults(defineProps<Props>(), {
@@ -21,11 +19,8 @@
     disabled: false,
     clearable: true,
     variant: 'default',
-    size: 'md',
   })
 
-  const heightClass = computed(() => (props.size === 'sm' ? 'h-10' : 'h-11'))
-
   const emit = defineEmits<{
     'update:modelValue': [value: string]
   }>()
@@ -48,7 +43,7 @@
       searchInputVariants({
         variant: props.variant,
         disabled: props.disabled,
-        class: [props.class, 'rounded-full', heightClass],
+        class: [props.class, 'rounded-full'],
       })
     "
     :disabled="disabled"
