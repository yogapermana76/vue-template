--- /Users/yogapermana/Desktop/vite-vue-template/src/components/ui/swipeable-tabs/SwipeableTabs.vue	2026-07-07 22:06:48
+++ /Users/yogapermana/Desktop/reward-loyalty-microapps-v2/src/components/ui/swipeable-tabs/SwipeableTabs.vue	2026-05-11 11:27:17
@@ -17,7 +17,6 @@
     swipeThreshold: 50,
     swipeVelocityThreshold: 0.3,
     heightOffset: '0px',
-    scrollFlow: false,
   })
 
   const emit = defineEmits<{
@@ -37,18 +36,11 @@
 
   // Computed
   // Only apply maxWidth on desktop (viewport wider than maxWidth)
-  const containerStyle = computed<CSSProperties>(() => {
-    if (props.scrollFlow) {
-      // In scroll-flow mode, let the container take natural height
-      return responsiveMaxWidth.value ? { maxWidth: responsiveMaxWidth.value } : {}
-    }
-    // Default fixed positioning mode
-    return {
-      top: props.heightOffset,
-      height: `calc(100dvh - ${props.heightOffset})`,
-      ...(responsiveMaxWidth.value && { maxWidth: responsiveMaxWidth.value }),
-    }
-  })
+  const containerStyle = computed<CSSProperties>(() => ({
+    top: props.heightOffset,
+    height: `calc(100dvh - ${props.heightOffset})`,
+    ...(responsiveMaxWidth.value && { maxWidth: responsiveMaxWidth.value }),
+  }))
 
   // Methods
   const setActiveTab = (key: string) => {
@@ -66,7 +58,6 @@
   // Context
   const context: SwipeableTabsContext = {
     tabs: props.tabs,
-    scrollFlow: props.scrollFlow,
     ...swipeableTabs,
     setActiveTab,
     setActiveIndex,
@@ -84,13 +75,7 @@
 
 <template>
   <div
-    :class="
-      cn(
-        'swipeable-tabs mx-auto flex flex-col',
-        !props.scrollFlow && 'fixed inset-x-0 bottom-0',
-        props.class,
-      )
-    "
+    :class="cn('swipeable-tabs fixed inset-x-0 bottom-0 mx-auto flex flex-col', props.class)"
     :style="containerStyle"
     data-slot="swipeable-tabs"
   >
